#!/usr/bin/env ruby

require 'rubygems'
if File.exists? File.join(File.dirname(__FILE__), "../.git") then
    $:.unshift File.expand_path(File.join(File.dirname(__FILE__), "../lib"))
end
require 'hcc'
